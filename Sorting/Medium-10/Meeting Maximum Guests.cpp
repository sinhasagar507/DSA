/* Meeting Maximum Guests */

/* 
	A variation of the count-intervals question. The logic can be used here
*/	

#include<bits/stdc++.h>
using namespace std;

int main()
{  
    // int n;
    // cin >> n;
//     
    // vector<pair<int, int>> v(n);
    // unordered_set<int> s;
//     
    // for(int i = 0; i < n; i++)
    // {
    	// int a, b;
    	// cin >> a >> b;
    	// v[i].first = a;
    	// v[i].second = b;
    // }
//     	
    // sort(v.begin(), v.end());
//     
    // int ans = 1, count = 1;
    // int arr = v[0].first, dep = v[0].second;
//     
    // for(int i = 0; i < n; i++)
    // {
    	// int element = v[i].second;
//     	
    	// if(s.find(element) == s.end())
    	// {
    		// s.insert(e);
//     		
	    	// while(element >= v[i].first)
	    	// {
	    		// if(s.find(element) == s.end())
	    		// {
	    			// s.insert(element);
	    			// element--;
	    		// }
// 	    		
	    		// else
	    			// break;
	    	// }
	    // }
    // }
//     
 	// cout << ans << endl;
// 	
	   return 0;
}
